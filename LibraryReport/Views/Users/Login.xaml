<Window x:Class="LibraryReport.Views.Users.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryReport.Views.Users"
        mc:Ignorable="d"
        Title="Login" Height="148.98" Width="300">
    <StackPanel Margin="0,0,0,0">
        <Grid Margin="10" Height="66">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="Login:"/>
            <TextBox x:Name="loginField" Grid.Row="0" Grid.Column="1" />
            <Label Grid.Row="1" Content="Password:"/>
            <PasswordBox x:Name="passwordField" Grid.Row="1" Grid.Column="1" />
        </Grid>
        <Button x:Name="loginBtn" Content="Log in" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="50" Click="Login_Click"/>
    </StackPanel>
</Window>
